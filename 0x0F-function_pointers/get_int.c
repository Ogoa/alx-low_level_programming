#include "function_pointers.h"

/**
 * get_int - Searches if a given integer matches a specified value
 * @num: The number being evaluated and compared
 *
 * Return: 1 if 'num' matches the specified number
 * 0 if no match is found
 */
int get_int(int num)
{
	return (num == 98);
}
